#ifndef INSTRUCTIONTABLES_H
#define INSTRUCTIONTABLES_H

char* destTable(char* dest, unsigned int lineNumber);
char* compTable(char* dest, unsigned int lineNumber);
char* jumpTable(char* dest, unsigned int lineNumber);

#endif